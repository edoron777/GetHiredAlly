FIX: Update FloatingSummaryBadge to use official severity colors

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PROBLEM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FloatingSummaryBadge is using wrong colors (Tailwind defaults).
Must use the OFFICIAL severity colors from severityColors.ts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OFFICIAL COLORS (from CV_Optimizer_Severity_Color_Convention.md)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

| Severity  | Hex Code  | Color Name     |
|-----------|-----------|----------------|
| critical  | #990033   | Maroon         |
| important | #990099   | Purple/Magenta |
| consider  | #1E5A85   | Navy Blue      |
| polish    | #008080   | Teal           |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK: Update FloatingSummaryBadge.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Option A: Import from severityColors.ts (if it exists)
```tsx
import { SEVERITY_COLORS } from '@/constants/severityColors';

// Use in component:
<span 
  className="w-3 h-3 rounded-full" 
  style={{ backgroundColor: SEVERITY_COLORS[severity].primary }}
/>
```

Option B: Define inline with correct hex values

Replace the SEVERITY_COLORS constant in FloatingSummaryBadge.tsx:
```tsx
// WRONG (current):
const SEVERITY_COLORS = {
  critical: { dot: 'bg-red-500' },
  important: { dot: 'bg-orange-500' },
  consider: { dot: 'bg-blue-500' },
  polish: { dot: 'bg-gray-400' },
};

// CORRECT (fix to):
const SEVERITY_COLORS = {
  critical: { 
    primary: '#990033',
    background: 'rgba(153, 0, 51, 0.1)',
  },
  important: { 
    primary: '#990099',
    background: 'rgba(153, 0, 153, 0.1)',
  },
  consider: { 
    primary: '#1E5A85',
    background: 'rgba(30, 90, 133, 0.1)',
  },
  polish: { 
    primary: '#008080',
    background: 'rgba(0, 128, 128, 0.1)',
  },
};
```

Then update the JSX to use style instead of className:
```tsx
// WRONG:
<span className={`w-3 h-3 rounded-full ${SEVERITY_COLORS[severity]?.dot}`}></span>

// CORRECT:
<span 
  className="w-3 h-3 rounded-full" 
  style={{ backgroundColor: SEVERITY_COLORS[severity]?.primary || '#6B7280' }}
></span>
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ALSO CHECK: Text labels in expanded view
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The severity labels should also use correct colors:
```tsx
// Text color for severity labels:
<div 
  className="text-xs font-semibold uppercase mb-1"
  style={{ color: SEVERITY_COLORS[severity]?.primary }}
>
  {severity} ({issueList.length})
</div>
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETION CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Critical dot is #990033 (Maroon)
[ ] Important dot is #990099 (Purple)
[ ] Consider dot is #1E5A85 (Navy Blue)
[ ] Polish dot is #008080 (Teal)
[ ] Text labels match dot colors
[ ] Colors match rest of CV Optimizer UI
```

---

### Visual Reference:
```
┌──────────────────────────────────┐
│ 📌 General Issues                │
│ ─────────────────────────────────│
│                                  │
│ 🔴 Important (3)  ← #990099      │
│ ├─ CV is Too Long                │
│ ├─ Missing LinkedIn URL          │
│ └─ Missing GitHub Profile        │
│                                  │
│ 🔵 Consider (3)   ← #1E5A85      │
│ ├─ Job Description Too Long      │
│ ├─ Old Roles Too Much Detail     │
│ └─ Education Lacks Detail        │
│                                  │
└──────────────────────────────────┘