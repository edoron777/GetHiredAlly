FIX: Marker circles are too small and duplicated

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BUGS FOUND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. MARKERS TOO SMALL
   - Current: Tiny dots barely visible
   - Expected: Clear circles (at least 12px / w-3 h-3)

2. MARKERS DUPLICATED
   - Current: Each issue shows 2 markers
   - Expected: Each issue shows 1 marker

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 1: Find the Marker Rendering Code
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search for where the circle markers are rendered in Document View.

Likely files:
- client/src/components/cv-optimizer/DocumentView/CVDocument.tsx
- client/src/components/cv-optimizer/DocumentView/TextMarker.tsx
- Any file with "marker" or "highlight" logic

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 2: Fix Duplicate Markers
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find WHY markers appear twice. Possible causes:

A) Marker rendered both BEFORE and AFTER the text
B) Two different components rendering markers
C) Map/loop running twice
D) Issue appearing in list twice

Debug approach:
1. Add console.log to marker rendering
2. Check if same issue appears twice in issues array
3. Check if marker is added in multiple places

Fix: Ensure only ONE marker per issue location.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 3: Fix Marker Size
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find the CSS for marker circles and increase size:

WRONG (too small):
```css
.marker-dot {
  width: 6px;   /* Too small! */
  height: 6px;
}
```

CORRECT:
```css
.marker-dot {
  width: 12px;  /* Or w-3 in Tailwind */
  height: 12px; /* Or h-3 in Tailwind */
}
```

Or in Tailwind:
```tsx
// WRONG:
<span className="w-1.5 h-1.5 rounded-full" />

// CORRECT:
<span className="w-3 h-3 rounded-full" />
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 4: Verify Marker Position
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Confirm markers appear on LEFT side of text (not inside):

WRONG:
  "I ● bridge the gap..."

CORRECT:
  "● I bridge the gap..."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXPECTED RESULT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before:
  ● I bridge the gap...  ← small, might be duplicate somewhere
  
After:
  🟣 I bridge the gap...  ← ONE marker, clear size (12px), correct color

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETION CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Found duplicate marker cause
[ ] Fixed - only ONE marker per issue
[ ] Increased marker size to 12px (w-3 h-3)
[ ] Markers appear on LEFT of text
[ ] Colors are correct (#990099 purple, #1E5A85 blue)
[ ] No visual layout breaks

Report what was causing the duplicate and how it was fixed.