═══════════════════════════════════════════════════════════════════════════════
🔧 SURGICAL FIX: URL Linkification in TextMarker
═══════════════════════════════════════════════════════════════════════════════
Type: Bug Fix
Priority: HIGH
Risk: LOW

───────────────────────────────────────────────────────────────────────────────
FILE TO MODIFY
───────────────────────────────────────────────────────────────────────────────

File: client/src/components/common/DocumentEditor/renderers/TextRenderer.tsx

───────────────────────────────────────────────────────────────────────────────
PROBLEM
───────────────────────────────────────────────────────────────────────────────

Current code (Lines 22-43):
```tsx
if (markers.length === 0) {
  return (
    <div className="text-content">
      <div className="text-preformatted">{linkifyContent(content)}</div>
    </div>
  )
}

return (
  <div className="text-content">
    <TextMarker
      content={content}  // ❌ Raw content - URLs not linkified!
      markers={markers}
      ...
    />
  </div>
)
```

When markers exist, URLs are NOT linkified.

───────────────────────────────────────────────────────────────────────────────
SOLUTION
───────────────────────────────────────────────────────────────────────────────

Option A: Linkify content BEFORE passing to TextMarker

Change this:
```tsx
<TextMarker
  content={content}
  markers={markers}
  ...
/>
```

To this:
```tsx
<TextMarker
  content={content}
  markers={markers}
  linkifyUrls={true}  // Add prop to enable URL linkification
  ...
/>
```

Then update TextMarker component to handle linkifyUrls prop.

───────────────────────────────────────────────────────────────────────────────
Option B: Pre-process content (Simpler)

If TextMarker renders content as HTML, linkify BEFORE passing:
```tsx
import { linkifyContent } from '../utils/linkifier';

// Inside the component
const processedContent = linkifyContent(content);

return (
  <div className="text-content">
    <TextMarker
      content={processedContent}  // Already linkified
      markers={markers}
      ...
    />
  </div>
)
```

───────────────────────────────────────────────────────────────────────────────
INVESTIGATION NEEDED
───────────────────────────────────────────────────────────────────────────────

Before implementing, show me:
1. Full TextMarker component code
2. How TextMarker renders the content prop
3. What linkifyContent() returns (string or JSX?)

Then choose the appropriate solution.

───────────────────────────────────────────────────────────────────────────────
VERIFICATION
───────────────────────────────────────────────────────────────────────────────

After fix:
1. Upload CV with LinkedIn URL
2. Open Document View
3. CV should have issues (markers exist)
4. LinkedIn URL should be clickable

═══════════════════════════════════════════════════════════════════════════════