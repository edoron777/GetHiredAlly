═══════════════════════════════════════════════════════════
CREATE PLEASANT SCANNER SOUNDS WITH WEB AUDIO API
═══════════════════════════════════════════════════════════

PROBLEM:
Current scanner sounds are harsh/scary - sound like errors.
We need pleasant, professional sounds.

GOAL:
Create a reusable sound utility using Web Audio API.
NO file uploads needed - sounds generated by code.

───────────────────────────────────────────────────────────
TASK: Create Sound Utility File
───────────────────────────────────────────────────────────

CREATE FILE: client/src/utils/scannerSounds.js

Content:

/**
 * Scanner Sound Effects - Web Audio API
 * Pleasant, professional sounds for scanning animations
 * No external files needed
 */

class ScannerSounds {
  constructor() {
    this.audioContext = null;
    this.enabled = true;
  }

  // Initialize audio context (must be called after user interaction)
  init() {
    if (!this.audioContext) {
      this.audioContext = new (window.AudioContext || window.webkitAudioContext)();
    }
    return this.audioContext;
  }

  // Enable/disable sounds
  setEnabled(enabled) {
    this.enabled = enabled;
  }

  /**
   * SCAN START SOUND
   * Soft ascending chime - indicates beginning
   * Like a gentle "ding" notification
   */
  playStartSound() {
    if (!this.enabled) return;
    
    const ctx = this.init();
    const now = ctx.currentTime;

    // Create oscillator for tone
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();

    osc.connect(gain);
    gain.connect(ctx.destination);

    // Ascending tone (C5 to E5)
    osc.type = 'sine';
    osc.frequency.setValueAtTime(523, now);        // C5
    osc.frequency.setValueAtTime(659, now + 0.15); // E5

    // Gentle fade in and out
    gain.gain.setValueAtTime(0, now);
    gain.gain.linearRampToValueAtTime(0.3, now + 0.05);
    gain.gain.linearRampToValueAtTime(0.2, now + 0.15);
    gain.gain.linearRampToValueAtTime(0, now + 0.4);

    osc.start(now);
    osc.stop(now + 0.4);
  }

  /**
   * SCAN PROGRESS SOUND (optional)
   * Subtle tick for each block scanned
   * Very quiet, non-intrusive
   */
  playTickSound() {
    if (!this.enabled) return;

    const ctx = this.init();
    const now = ctx.currentTime;

    const osc = ctx.createOscillator();
    const gain = ctx.createGain();

    osc.connect(gain);
    gain.connect(ctx.destination);

    osc.type = 'sine';
    osc.frequency.setValueAtTime(1000, now);

    // Very short, very quiet
    gain.gain.setValueAtTime(0.05, now);
    gain.gain.linearRampToValueAtTime(0, now + 0.05);

    osc.start(now);
    osc.stop(now + 0.05);
  }

  /**
   * SCAN COMPLETE SOUND
   * Pleasant success chime - triumphant but not loud
   * Two-tone ascending chord
   */
  playCompleteSound() {
    if (!this.enabled) return;

    const ctx = this.init();
    const now = ctx.currentTime;

    // First tone (C5)
    const osc1 = ctx.createOscillator();
    const gain1 = ctx.createGain();
    osc1.connect(gain1);
    gain1.connect(ctx.destination);
    osc1.type = 'sine';
    osc1.frequency.setValueAtTime(523, now); // C5
    gain1.gain.setValueAtTime(0, now);
    gain1.gain.linearRampToValueAtTime(0.25, now + 0.05);
    gain1.gain.linearRampToValueAtTime(0, now + 0.5);
    osc1.start(now);
    osc1.stop(now + 0.5);

    // Second tone (E5) - slightly delayed
    const osc2 = ctx.createOscillator();
    const gain2 = ctx.createGain();
    osc2.connect(gain2);
    gain2.connect(ctx.destination);
    osc2.type = 'sine';
    osc2.frequency.setValueAtTime(659, now + 0.1); // E5
    gain2.gain.setValueAtTime(0, now + 0.1);
    gain2.gain.linearRampToValueAtTime(0.25, now + 0.15);
    gain2.gain.linearRampToValueAtTime(0, now + 0.6);
    osc2.start(now + 0.1);
    osc2.stop(now + 0.6);

    // Third tone (G5) - completing the chord
    const osc3 = ctx.createOscillator();
    const gain3 = ctx.createGain();
    osc3.connect(gain3);
    gain3.connect(ctx.destination);
    osc3.type = 'sine';
    osc3.frequency.setValueAtTime(784, now + 0.2); // G5
    gain3.gain.setValueAtTime(0, now + 0.2);
    gain3.gain.linearRampToValueAtTime(0.3, now + 0.25);
    gain3.gain.linearRampToValueAtTime(0, now + 0.8);
    osc3.start(now + 0.2);
    osc3.stop(now + 0.8);
  }

  /**
   * ERROR SOUND
   * Gentle alert - NOT scary
   * Low tone, soft - indicates "attention needed"
   */
  playErrorSound() {
    if (!this.enabled) return;

    const ctx = this.init();
    const now = ctx.currentTime;

    const osc = ctx.createOscillator();
    const gain = ctx.createGain();

    osc.connect(gain);
    gain.connect(ctx.destination);

    // Low, soft tone (not harsh)
    osc.type = 'sine';
    osc.frequency.setValueAtTime(330, now);  // E4
    osc.frequency.setValueAtTime(294, now + 0.15); // D4 (descending)

    gain.gain.setValueAtTime(0, now);
    gain.gain.linearRampToValueAtTime(0.2, now + 0.05);
    gain.gain.linearRampToValueAtTime(0.15, now + 0.2);
    gain.gain.linearRampToValueAtTime(0, now + 0.4);

    osc.start(now);
    osc.stop(now + 0.4);
  }
}

// Export singleton instance
export const scannerSounds = new ScannerSounds();

// Export individual functions for convenience
export const playStartSound = () => scannerSounds.playStartSound();
export const playCompleteSound = () => scannerSounds.playCompleteSound();
export const playErrorSound = () => scannerSounds.playErrorSound();
export const playTickSound = () => scannerSounds.playTickSound();
export const setSoundEnabled = (enabled) => scannerSounds.setEnabled(enabled);

───────────────────────────────────────────────────────────
TASK: Update Scanner Component to Use New Sounds
───────────────────────────────────────────────────────────

FILE: client/src/components/CVScanningPage.tsx (or similar scanner component)

1. ADD import at top:

import { playStartSound, playCompleteSound } from '@/utils/scannerSounds';

2. FIND where scan starts and ADD:

// When scanning begins
useEffect(() => {
  playStartSound();
}, []);

3. FIND where scan completes and ADD:

// When scanning completes
const handleScanComplete = () => {
  playCompleteSound();
  // ... rest of completion logic
};

4. REMOVE old sound code if it exists (the scary sounds)

───────────────────────────────────────────────────────────
OPTIONAL: Add Sound Toggle for Users
───────────────────────────────────────────────────────────

Some users may not want sounds. Add a mute option:

import { setSoundEnabled } from '@/utils/scannerSounds';

// In component or settings
const [soundEnabled, setSoundEnabledState] = useState(true);

const toggleSound = () => {
  const newState = !soundEnabled;
  setSoundEnabledState(newState);
  setSoundEnabled(newState);
};

// UI
<button onClick={toggleSound}>
  {soundEnabled ? '🔊' : '🔇'}
</button>

───────────────────────────────────────────────────────────
SOUND CHARACTERISTICS
───────────────────────────────────────────────────────────

| Sound | Notes | Duration | Feeling |
|-------|-------|----------|---------|
| Start | C5 → E5 ascending | 0.4s | "Let's begin" |
| Complete | C5, E5, G5 chord | 0.8s | "Success!" |
| Error | E4 → D4 descending | 0.4s | "Attention" (not scary) |
| Tick | Quick 1000Hz | 0.05s | Subtle progress |

All sounds use:
- Sine waves (smooth, not harsh)
- Low volume (0.2-0.3 max)
- Gentle fade in/out (no clicks)

───────────────────────────────────────────────────────────
VERIFICATION
───────────────────────────────────────────────────────────

After implementation:
1. Go to Perfect Your CV
2. Upload a CV
3. Click "Start Scan"
4. HEAR: Pleasant ascending chime (start sound)
5. Wait for scan to complete
6. HEAR: Pleasant success chord (complete sound)

The sounds should be:
- Pleasant, not annoying
- Professional, not childish
- Quiet enough to not startle
- Clear enough to notice

───────────────────────────────────────────────────────────
DO NOT:
───────────────────────────────────────────────────────────

- Do NOT use harsh square/sawtooth waves
- Do NOT use high volume (keep below 0.3)
- Do NOT make sounds too long (under 1 second)
- Do NOT play sounds without fade in/out (causes clicks)