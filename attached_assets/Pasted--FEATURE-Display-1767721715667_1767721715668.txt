═══════════════════════════════════════════════════════════════════════════════
🔧 FEATURE: Display Impact Types in TipBox (ATS / Human / Best Practice)
═══════════════════════════════════════════════════════════════════════════════
Version: ReplitPromptProtocol v2.5
Type: Feature Enhancement
Priority: MEDIUM
Risk Level: LOW (additive change, no breaking changes)

───────────────────────────────────────────────────────────────────────────────
CONTEXT
───────────────────────────────────────────────────────────────────────────────

We added a new column `impact_types` to the `cv_issue_types` table in the 
database. This column contains a JSON array classifying each issue by WHO 
it affects:

- 🤖 ATS systems (parsing/ranking issues)
- 👤 Human recruiters (perception/impression issues)  
- 📋 Best practices (industry recommendations)

The data is in the database but the frontend doesn't display it yet.

Database column: cv_issue_types.impact_types (jsonb)
Example values:
- '["ats_high", "best_practice"]'
- '["human_high", "best_practice"]'
- '["ats_medium", "human_medium", "best_practice"]'

Impact type values:
- "ats_high" = ATS parsing will likely fail
- "ats_medium" = ATS may have minor issues
- "human_high" = Recruiter likely to reject
- "human_medium" = Creates negative impression
- "human_low" = Minor perception issue
- "best_practice" = Industry recommendation

───────────────────────────────────────────────────────────────────────────────
OBJECTIVE
───────────────────────────────────────────────────────────────────────────────

1. Backend: Include `impact_types` in API response when returning issues
2. Frontend: Display impact type badges in TipBox header
3. Frontend: Show impact-specific explanation in "WHY THIS MATTERS" section

───────────────────────────────────────────────────────────────────────────────
TASK 1: BACKEND - Include impact_types in API Response
───────────────────────────────────────────────────────────────────────────────

INVESTIGATION:
Find where cv_issue_types data is fetched and returned to frontend.
Look in:
- backend/common/detection/ (issue detection)
- backend/api/ or backend/routes/ (API endpoints)
- Any file that queries cv_issue_types table

CHANGE REQUIRED:
When fetching issue data from cv_issue_types, include the `impact_types` column
in the SELECT query and return it in the API response.

Example - if current query is:
```sql
SELECT issue_code, display_name, description, severity, static_tip...
FROM cv_issue_types
```

Change to:
```sql
SELECT issue_code, display_name, description, severity, static_tip, impact_types...
FROM cv_issue_types
```

Ensure the API response includes:
```json
{
  "issue_code": "LENGTH_CV_TOO_LONG",
  "display_name": "CV is Too Long",
  "impact_types": ["human_high", "best_practice"],
  ...
}
```

───────────────────────────────────────────────────────────────────────────────
TASK 2: FRONTEND - Add Impact Type Badges to TipBox Header
───────────────────────────────────────────────────────────────────────────────

FILE: client/src/pages/cv-optimizer/ResultsPage.tsx (or TipBox component)

Add impact badges below the issue title in TipBox:

BEFORE:
┌─────────────────────────────────────────────────────────────────┐
│  CV is Too Long                                    [IMPORTANT]  │
│                                                                 │
│  WHY THIS MATTERS:                                              │
└─────────────────────────────────────────────────────────────────┘

AFTER:
┌─────────────────────────────────────────────────────────────────┐
│  CV is Too Long                                    [IMPORTANT]  │
│  👤 Human Impact  •  📋 Best Practice                           │
│                                                                 │
│  WHY THIS MATTERS:                                              │
└─────────────────────────────────────────────────────────────────┘

IMPLEMENTATION:

1. Create a helper function to render impact badges:
```typescript
// Impact type display configuration
const IMPACT_DISPLAY = {
  ats_high: { icon: '🤖', label: 'ATS Critical', color: 'bg-red-100 text-red-800' },
  ats_medium: { icon: '🤖', label: 'ATS Impact', color: 'bg-orange-100 text-orange-800' },
  human_high: { icon: '👤', label: 'Human Critical', color: 'bg-red-100 text-red-800' },
  human_medium: { icon: '👤', label: 'Human Impact', color: 'bg-yellow-100 text-yellow-800' },
  human_low: { icon: '👤', label: 'Minor Impact', color: 'bg-gray-100 text-gray-600' },
  best_practice: { icon: '📋', label: 'Best Practice', color: 'bg-blue-100 text-blue-800' }
};

const renderImpactBadges = (impactTypes: string[]) => {
  if (!impactTypes || impactTypes.length === 0) return null;
  
  return (
    <div className="flex flex-wrap gap-2 mt-1 mb-3">
      {impactTypes.map((type) => {
        const config = IMPACT_DISPLAY[type];
        if (!config) return null;
        return (
          <span 
            key={type}
            className={`inline-flex items-center px-2 py-0.5 rounded text-xs font-medium ${config.color}`}
          >
            {config.icon} {config.label}
          </span>
        );
      })}
    </div>
  );
};
```

2. Add badges to TipBox after the title/severity line:
```tsx
{/* After issue title */}
{issue.impact_types && renderImpactBadges(issue.impact_types)}
```

───────────────────────────────────────────────────────────────────────────────
TASK 3: FRONTEND - Update "WHY THIS MATTERS" Section
───────────────────────────────────────────────────────────────────────────────

FILE: client/src/pages/cv-optimizer/ResultsPage.tsx (buildTipBoxSections function)

Update the "WHY THIS MATTERS" section to show impact-specific explanations.

IMPLEMENTATION:

1. Create impact explanation generator:
```typescript
const getImpactExplanation = (impactTypes: string[]): string[] => {
  const explanations: string[] = [];
  
  // Check for ATS impact
  if (impactTypes?.includes('ats_high')) {
    explanations.push('🤖 ATS SYSTEMS: This issue may cause parsing failure. Your CV content could be lost or scrambled before a human sees it.');
  } else if (impactTypes?.includes('ats_medium')) {
    explanations.push('🤖 ATS SYSTEMS: This may affect how ATS systems parse or rank your CV.');
  } else if (impactTypes && !impactTypes.some(t => t.startsWith('ats_'))) {
    explanations.push('🤖 ATS SYSTEMS: No impact. ATS can parse this correctly.');
  }
  
  // Check for Human impact
  if (impactTypes?.includes('human_high')) {
    explanations.push('👤 HUMAN READER: Recruiters spend only 6-7 seconds scanning CVs. This issue may cause immediate rejection.');
  } else if (impactTypes?.includes('human_medium')) {
    explanations.push('👤 HUMAN READER: This may create a negative impression and reduce your chances.');
  } else if (impactTypes?.includes('human_low')) {
    explanations.push('👤 HUMAN READER: Minor issue that most recruiters may not notice.');
  }
  
  return explanations;
};
```

2. Update buildTipBoxSections to include impact explanations:

In the section where "WHY THIS MATTERS" is built, add:
```typescript
// Add impact-specific explanations
const impactExplanations = getImpactExplanation(issue.impact_types);
if (impactExplanations.length > 0) {
  sections.push({
    type: 'impact-explanation',
    label: 'IMPACT ANALYSIS',
    content: impactExplanations.join('\n\n')
  });
}
```

───────────────────────────────────────────────────────────────────────────────
VISUAL DESIGN SPECIFICATIONS
───────────────────────────────────────────────────────────────────────────────

Badge Colors (Tailwind CSS):
- ATS Critical (ats_high): bg-red-100 text-red-800 border-red-200
- ATS Impact (ats_medium): bg-orange-100 text-orange-800 border-orange-200
- Human Critical (human_high): bg-red-100 text-red-800 border-red-200
- Human Impact (human_medium): bg-yellow-100 text-yellow-800 border-yellow-200
- Minor Impact (human_low): bg-gray-100 text-gray-600 border-gray-200
- Best Practice: bg-blue-100 text-blue-800 border-blue-200

Badge Style:
- Rounded corners (rounded)
- Small text (text-xs)
- Medium font weight (font-medium)
- Small padding (px-2 py-0.5)
- Icon + Label format

───────────────────────────────────────────────────────────────────────────────
EXAMPLE: COMPLETE TIPBOX AFTER IMPLEMENTATION
───────────────────────────────────────────────────────────────────────────────

Issue: CV is Too Long (impact_types: ["human_high", "best_practice"])

┌─────────────────────────────────────────────────────────────────────────────┐
│  CV is Too Long                                           [IMPORTANT]       │
│                                                                             │
│  [👤 Human Critical]  [📋 Best Practice]                                    │
│                                                                             │
│  ───────────────────────────────────────────────────────────────────────    │
│                                                                             │
│  IMPACT ANALYSIS                                                            │
│  🤖 ATS SYSTEMS: No impact. ATS can parse this correctly.                   │
│                                                                             │
│  👤 HUMAN READER: Recruiters spend only 6-7 seconds scanning CVs.           │
│     This issue may cause immediate rejection.                               │
│                                                                             │
│  ───────────────────────────────────────────────────────────────────────    │
│                                                                             │
│  WHY THIS MATTERS                                                           │
│  Your CV has 1838 words (maximum: 1200). Long CVs get skimmed, not read.    │
│                                                                             │
│  ───────────────────────────────────────────────────────────────────────    │
│                                                                             │
│  HOW TO FIX                                                                 │
│  Target length: 1 page for less than 10 years experience...                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

───────────────────────────────────────────────────────────────────────────────
ACCEPTANCE CRITERIA
───────────────────────────────────────────────────────────────────────────────

1. ✅ API returns impact_types array for each issue
2. ✅ TipBox displays impact badges below issue title
3. ✅ Badges show appropriate icon and color based on impact type
4. ✅ "IMPACT ANALYSIS" section shows ATS and Human explanations
5. ✅ Issues with no ATS impact show "No impact" message
6. ✅ Existing TipBox functionality unchanged (HOW TO FIX, GOOD EXAMPLE, etc.)

───────────────────────────────────────────────────────────────────────────────
FILES TO MODIFY
───────────────────────────────────────────────────────────────────────────────

| File | Changes |
|------|---------|
| Backend API/routes file | Add impact_types to SELECT query |
| client/src/pages/cv-optimizer/ResultsPage.tsx | Add badge rendering, impact explanations |

───────────────────────────────────────────────────────────────────────────────
DO NOT MODIFY
───────────────────────────────────────────────────────────────────────────────

- Database schema (already updated)
- Issue detection logic
- Existing TipBox sections (HOW TO FIX, GOOD EXAMPLE, etc.)
- Severity badges and colors

───────────────────────────────────────────────────────────────────────────────
TESTING
───────────────────────────────────────────────────────────────────────────────

After implementation:

1. Upload a CV and run analysis
2. Click on "CV is Too Long" issue
   - Should show: [👤 Human Critical] [📋 Best Practice] badges
   - Should show: "🤖 ATS SYSTEMS: No impact" in explanation

3. Click on "Multiple Columns" issue (if detected)
   - Should show: [🤖 ATS Critical] [📋 Best Practice] badges
   - Should show: "🤖 ATS SYSTEMS: This issue may cause parsing failure"

4. Click on "Trailing Whitespace" issue (if detected)
   - Should show: [📋 Best Practice] badge only
   - Should show minimal impact explanation

═══════════════════════════════════════════════════════════════════════════════