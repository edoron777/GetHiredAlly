╔══════════════════════════════════════════════════════════════════════════════╗
║  🔍 RFI: Why Are Visual Markers Not Showing on CV Text?                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

CONTEXT:
- Issues ARE detected (12 found)
- Issue popup WORKS (shows details when clicked)
- CV text in Document View has NO colored markers/highlights

The highlighting of issues ON the CV text is not working.

───────────────────────────────────────────────────────────────────────────────
TASK 1: Find the Document View component
───────────────────────────────────────────────────────────────────────────────

Which component renders the CV text in "Document View" tab?

Search:
```bash
grep -rn "Document View" client/src/
grep -rn "DocumentView" client/src/
```

Show me the file path and component name.

───────────────────────────────────────────────────────────────────────────────
TASK 2: Check if highlighting logic exists
───────────────────────────────────────────────────────────────────────────────

In the Document View component, look for:

1. Does it receive `issues` as a prop?
2. Is there any code that applies highlights/markers to text?
3. Is TextMarker component being used?

Search:
```bash
grep -rn "TextMarker" client/src/
grep -rn "highlight" client/src/components/cv-optimizer/
grep -rn "marker" client/src/components/cv-optimizer/
```

───────────────────────────────────────────────────────────────────────────────
TASK 3: Check issue data structure
───────────────────────────────────────────────────────────────────────────────

When the API returns issues, do they include:
- `current` field (the text to highlight)?
- `line_number` field?
- `is_highlightable` field?

Check the API response in browser DevTools (Network tab) 
OR check the backend response format.

───────────────────────────────────────────────────────────────────────────────
TASK 4: Is highlighting implemented at all?
───────────────────────────────────────────────────────────────────────────────

Look at the CV Optimizer unified page and Document View:

1. Is there ANY code that takes issue text and highlights it in the CV?
2. Or is this feature NOT IMPLEMENTED YET?

If not implemented, say: "Highlighting not implemented"
If implemented but broken, show the code.

───────────────────────────────────────────────────────────────────────────────

DO NOT MODIFY - just investigate and report.

╚══════════════════════════════════════════════════════════════════════════════╝