## DEBUG & FIX: Dashboard Menu Link Not Working

### Problem Description
The "Dashboard" link appears in the user dropdown menu (top right corner), but when clicking on it, nothing happens. The link is not navigating anywhere.

### Current Database State
- User `edoron777@gmail.com` has `is_admin = true` and `role = 'admin'` in the database
- The Dashboard link SHOULD navigate admin users to an admin panel

### Your Task

**STEP 1: Find the Dashboard menu item**
Search the codebase for "Dashboard" in:
- Header components
- Navbar components  
- UserMenu components
- Any dropdown menu components

**STEP 2: Check what's wrong**
When you find the Dashboard menu item, check:
- Does it have an `onClick` handler?
- Does it have an `href` or `to` attribute?
- Is it wrapped in a `<Link>` component?
- Is it just a `<div>` or `<span>` with no navigation?

**STEP 3: Check if admin route exists**
Look in App.jsx or routes file:
- Is there a route for `/admin` or `/admin/dashboard`?
- Is there an AdminDashboard component?

**STEP 4: Fix the issues**

If the route doesn't exist, create it:
```jsx
// Create: client/src/pages/admin/AdminDashboard.jsx
import { useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '@/hooks/useAuth'; // or wherever auth hook is

export default function AdminDashboard() {
  const { user } = useAuth();
  const navigate = useNavigate();

  // Redirect non-admin users
  useEffect(() => {
    if (user && !user.is_admin) {
      navigate('/');
    }
  }, [user, navigate]);

  return (
    <div className="min-h-screen bg-[#FAF9F7] p-8">
      <div className="max-w-6xl mx-auto">
        <h1 className="text-3xl font-bold text-[#1E3A5F] mb-6">Admin Dashboard</h1>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div className="bg-white p-6 rounded-lg shadow">
            <h2 className="text-lg font-semibold mb-2">Total Users</h2>
            <p className="text-3xl font-bold text-[#1E3A5F]">--</p>
          </div>
          <div className="bg-white p-6 rounded-lg shadow">
            <h2 className="text-lg font-semibold mb-2">AI API Calls</h2>
            <p className="text-3xl font-bold text-[#1E3A5F]">--</p>
          </div>
          <div className="bg-white p-6 rounded-lg shadow">
            <h2 className="text-lg font-semibold mb-2">Total Cost</h2>
            <p className="text-3xl font-bold text-[#1E3A5F]">$--</p>
          </div>
        </div>
        <p className="mt-8 text-gray-600">Full admin features coming soon.</p>
      </div>
    </div>
  );
}
```

Add the route in App.jsx:
```jsx
import AdminDashboard from './pages/admin/AdminDashboard';

// Inside Routes:
<Route path="/admin" element={<AdminDashboard />} />
```

Fix the Dashboard menu link (example fix):
```jsx
// Change from something like:
<DropdownMenuItem>Dashboard</DropdownMenuItem>

// To:
<DropdownMenuItem onClick={() => navigate('/admin')}>
  Dashboard
</DropdownMenuItem>

// Or using Link:
<DropdownMenuItem asChild>
  <Link to="/admin">Dashboard</Link>
</DropdownMenuItem>
```

**STEP 5: Test**
After fixing, verify:
1. Clicking Dashboard navigates to /admin
2. The admin page displays correctly
3. Non-admin users cannot access /admin

### Important Notes
- The app uses React Router for navigation
- The app uses shadcn/ui components
- Keep existing styling (beige background #FAF9F7, navy blue #1E3A5F)
- The user dropdown is likely using shadcn's DropdownMenu component

### Show me what you find and fix!