╔══════════════════════════════════════════════════════════════════════════════╗
║  🔍 RFI: Why Are CV Issue Markers Not Displaying?                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

PROBLEM:
- Backend detects 12 issues (working)
- Frontend shows issue count in sidebar (working)
- Frontend does NOT show colored markers on CV text (broken)

───────────────────────────────────────────────────────────────────────────────
TASK 1: Check what data the frontend receives
───────────────────────────────────────────────────────────────────────────────

In browser DevTools (F12), go to Network tab, find the API call to 
/api/cv-optimizer/* and show me:

1. What issues array is returned?
2. Do issues have `current` field? (text to highlight)
3. Do issues have `line_number` field?
4. Do issues have `is_highlightable` field?

───────────────────────────────────────────────────────────────────────────────
TASK 2: Check TextMarker component usage
───────────────────────────────────────────────────────────────────────────────

Find where the CV text is displayed in Document View.

1. Which component renders the CV text?
2. Is TextMarker component being used?
3. Is it receiving the issues array?

Search for:
```bash
grep -rn "TextMarker" client/src/
grep -rn "is_highlightable" client/src/
grep -rn "Document View" client/src/
```

───────────────────────────────────────────────────────────────────────────────
TASK 3: Check if highlighting logic exists
───────────────────────────────────────────────────────────────────────────────

Find the component that shows the CV text and check:

1. Does it receive issues as props?
2. Does it apply any highlighting/markers?
3. Is there conditional rendering that might hide markers?

───────────────────────────────────────────────────────────────────────────────

DO NOT MODIFY - just investigate and report findings.

╚══════════════════════════════════════════════════════════════════════════════╝