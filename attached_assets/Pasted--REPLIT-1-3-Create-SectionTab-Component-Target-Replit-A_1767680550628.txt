💻 REPLIT-1#3: Create SectionTab Component
Target: Replit AI Agent
textIMPLEMENT: Create SectionTab component with editable dropdown

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONTEXT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Each CV section needs a colored tab on the left side (like notebook tabs).
The tab shows the section name and allows users to change the section type
if the automatic detection was wrong.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 1: Create SectionTab Component
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create: client/src/components/cv-optimizer/DocumentView/SectionTab.tsx
```tsx
import React, { useState } from 'react';
import { ChevronDown, Check } from 'lucide-react';
import { SectionType, SECTION_COLORS } from './StructureOverlay';

// Section type options for dropdown
const SECTION_OPTIONS: { value: SectionType; label: string }[] = [
  { value: 'CONTACT', label: 'Contact' },
  { value: 'SUMMARY', label: 'Summary' },
  { value: 'EXPERIENCE', label: 'Experience' },
  { value: 'EDUCATION', label: 'Education' },
  { value: 'SKILLS', label: 'Skills' },
  { value: 'CERTIFICATIONS', label: 'Certifications' },
  { value: 'PROJECTS', label: 'Projects' },
  { value: 'LANGUAGES', label: 'Languages' },
  { value: 'UNKNOWN', label: 'Unknown' },
];

interface SectionTabProps {
  sectionType: SectionType;
  lineRange: string;
  onTypeChange: (newType: SectionType) => void;
}

export const SectionTab: React.FC<SectionTabProps> = ({
  sectionType,
  lineRange,
  onTypeChange
}) => {
  const [isDropdownOpen, setIsDropdownOpen] = useState(false);
  const [currentType, setCurrentType] = useState(sectionType);
  
  const colors = SECTION_COLORS[currentType];
  
  const handleTypeSelect = (newType: SectionType) => {
    setCurrentType(newType);
    onTypeChange(newType);
    setIsDropdownOpen(false);
  };

  return (
    <div className="section-tab-wrapper relative">
      {/* Tab Button */}
      <button
        onClick={() => setIsDropdownOpen(!isDropdownOpen)}
        className="section-tab flex items-center gap-1 px-3 py-2 rounded-l-lg
                   text-white font-medium text-sm cursor-pointer
                   hover:brightness-110 transition-all shadow-md"
        style={{ 
          backgroundColor: colors.tab,
          writingMode: 'vertical-rl',
          textOrientation: 'mixed',
          transform: 'rotate(180deg)',
          minHeight: '100px',
        }}
      >
        <span className="uppercase tracking-wide">{currentType}</span>
        <ChevronDown className="w-3 h-3" />
      </button>
      
      {/* Line Range Badge */}
      <div 
        className="absolute bottom-2 left-1/2 transform -translate-x-1/2
                   text-xs text-white/80 whitespace-nowrap"
        style={{ writingMode: 'horizontal-tb' }}
      >
        {lineRange}
      </div>

      {/* Dropdown Menu */}
      {isDropdownOpen && (
        <div 
          className="absolute left-full top-0 ml-2 z-50
                     bg-white rounded-lg shadow-xl border border-gray-200
                     py-1 min-w-[160px]"
        >
          <div className="px-3 py-2 text-xs text-gray-500 border-b">
            Change section type:
          </div>
          {SECTION_OPTIONS.map((option) => (
            <button
              key={option.value}
              onClick={() => handleTypeSelect(option.value)}
              className="w-full px-3 py-2 text-left text-sm hover:bg-gray-100
                         flex items-center justify-between"
            >
              <div className="flex items-center gap-2">
                <span 
                  className="w-3 h-3 rounded-full"
                  style={{ backgroundColor: SECTION_COLORS[option.value].tab }}
                />
                <span>{option.label}</span>
              </div>
              {currentType === option.value && (
                <Check className="w-4 h-4 text-green-500" />
              )}
            </button>
          ))}
        </div>
      )}
      
      {/* Click outside to close */}
      {isDropdownOpen && (
        <div 
          className="fixed inset-0 z-40"
          onClick={() => setIsDropdownOpen(false)}
        />
      )}
    </div>
  );
};

export default SectionTab;
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 2: Tab Styling (Notebook Style)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The tab should look like a notebook divider tab:
- Vertical text (rotated 90 degrees)
- Colored background matching section
- Rounded on the left side
- Slightly extends outside the section box

Visual example:
  ┌────────────────────────────────────────────┐
┌─────┤                                            │
│     │  David Cohen CV | Email: dcohen@gmail.com  │
│ C   │                                            │
│ O   │                                            │
│ N   ├────────────────────────────────────────────┘
│ T   │
│ A   │
│ C   │
│ T   │
│     │
│ 1-1 │
└─────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETION CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Created SectionTab.tsx
[ ] Tab shows section name vertically
[ ] Tab colored by section type
[ ] Dropdown opens on click
[ ] Dropdown shows all section type options
[ ] Selected type has checkmark
[ ] Click outside closes dropdown
[ ] Line range displayed on tab