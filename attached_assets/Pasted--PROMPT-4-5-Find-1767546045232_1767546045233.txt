═══════════════════════════════════════════════════════════════════════════════
🎯 PROMPT 4#5: Find the Smoking Gun - Why Detection is Broken
═══════════════════════════════════════════════════════════════════════════════

⚠️ CRITICAL: This is the KEY investigation step.

───────────────────────────────────────────────────────────────────────────────
OBJECTIVE: Identify THE root cause of detection failure
───────────────────────────────────────────────────────────────────────────────

Based on PROMPT 1-3 findings, answer these questions:

───────────────────────────────────────────────────────────────────────────────
QUESTION 1: Is the DB being read correctly?
───────────────────────────────────────────────────────────────────────────────

Show where rules are loaded from DB:

grep -rn "supabase\|select\|fetch" backend/common/detection/rule_engine/ --include="*.py" -A 5

Are rules loaded? YES/NO
Evidence: __________

───────────────────────────────────────────────────────────────────────────────
QUESTION 2: Are DB rules being used or ignored?
───────────────────────────────────────────────────────────────────────────────

Add this debug to the engine:
```python
# At the start of rule evaluation
print(f"[ENGINE] Rule: {rule.issue_type}")
print(f"[ENGINE] DB config: {rule.detection_config}")
print(f"[ENGINE] Handler will use: {handler_type}")
```

Do handlers use the config from DB? YES/NO
Or do they use hardcoded logic? YES/NO
Evidence: __________

───────────────────────────────────────────────────────────────────────────────
QUESTION 3: Where does detection go WRONG?
───────────────────────────────────────────────────────────────────────────────

Pick ONE rule that should detect an issue but doesn't (or vice versa).

Rule chosen: __________
DB says: __________
Handler does: __________
Result: __________
WHY it fails: __________

───────────────────────────────────────────────────────────────────────────────
QUESTION 4: Is there OLD detection code interfering?
───────────────────────────────────────────────────────────────────────────────

Search for hardcoded detection:

grep -rn "def detect_\|issues.*append\|ISSUE_TYPE" backend/common/detection/ --include="*.py" | grep -v rule_engine | grep -v __pycache__

Is there old detection code running alongside new? YES/NO
Location: __________

───────────────────────────────────────────────────────────────────────────────

THE SMOKING GUN:

Based on investigation, THE root cause is:

[ ] A. DB rules are correct but handlers IGNORE the config
[ ] B. DB rules are WRONG (bad patterns, wrong logic)
[ ] C. Section parser doesn't provide correct text to handlers
[ ] D. OLD detection code overrides NEW rule engine
[ ] E. Handlers have BUGS that produce wrong results
[ ] F. Multiple issues: __________

EVIDENCE:
__________

SPECIFIC LOCATION OF BUG:
File: __________
Line: __________
What's wrong: __________

───────────────────────────────────────────────────────────────────────────────

Type "PROMPT 4#5 COMPLETE - SMOKING GUN FOUND" when done.
═══════════════════════════════════════════════════════════════════════════════